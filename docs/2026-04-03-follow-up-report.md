# 2026-04-03 遗留事项与后续建议报告

## 当前仍未完成的部分

- `frontend/assets/（SCU部分）VNFest中文版_V3.pdf` 的内容尚未完整融入首页或社团介绍页。
- 前端仍保留本地静态兜底内容，没有彻底切成“只读数据库”模式。
- 站点内容 CRUD 目前只有后端接口，没有后台管理页面。
- 数据库迁移已经执行，但如果后续继续扩 schema，仍需要建立更系统的迁移管理方式。

## 已知限制

- 站点内容与展示墙虽然已数据库化，但并非所有页面元素都已经从数据库读取。
- 个人空间当前仍是以固定用户名 `rubedo_room` 为展示对象，不是完整多用户空间流。
- 活动模块当前只补了最小编译面，不在本次数据库改造的主线范围内。

## 推荐后续顺序

- 优先把 PDF 中的社团历史与活动内容整理进 `/portal` 页面。
- 再决定是否移除前端本地静态兜底，改为强依赖数据库内容。
- 最后再补站点内容后台管理页，真正把新增 CRUD 用起来。

## 风险提醒

- 当前 `DB/schema.sql` 和增量 migration 同时存在，后续修改必须保持两边一致。
- 若未来继续新增内容表，建议建立统一的 migration 目录与执行规范，避免 schema 与运行库再次偏离。
