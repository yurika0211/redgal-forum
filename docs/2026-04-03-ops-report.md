# 2026-04-03 运维与环境操作报告

日期：2026-04-03

## 工作内容

### 1. Docker 容器操作

- 停止 `rubedo-backend`
- 停止 `rubedo-frontend`

目的：

- 关闭已运行的容器化前后端，避免与后续本地调试和端口检查互相干扰。

### 2. 本地进程操作

- 定位并停止当前项目直接运行的前端开发进程
- 定位并停止当前项目直接运行的后端开发进程

识别到的本地进程类型：

- `vite`
- `go run ./cmd/api`

目的：

- 保证当前仓库不存在遗留的前后端进程，避免误判服务状态和端口占用。

### 3. 数据库服务操作

- 启动 `docker compose` 下的 `postgres` 服务

目的：

- 为后续测试账号写入和数据库核对提供可用实例。

## 当前环境状态

- PostgreSQL：已启动并用于后续账号写入
- Docker 前端：已停止
- Docker 后端：已停止
- 本地直接运行的前端：已停止
- 本地直接运行的后端：已停止

## 备注

- 本次没有执行破坏性清理命令。
- 本次没有配置或推送任何远端仓库内容。
